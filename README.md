# Projekt zaliczeniowy Internet of Things

### Uruchomienie

```zsh
python -m venv .venv
source .venv/bin/activate (source ./venv/Scripts/activate dla systemu Windows)
pip install -r requirements.txt
python src/main.py
```